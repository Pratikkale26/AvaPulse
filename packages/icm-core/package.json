{
  "name": "@avapulse/icm-core",
  "version": "0.1.0",
  "description": "Teleporter/ICM message lifecycle engine: event decoding, messageID correlation, lifecycle state machine",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "viem": "^2.38.0"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^3"
  }
}
